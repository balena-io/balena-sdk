// Generated by CoffeeScript 1.12.5
var dtPredicate, find, findBySlug, includes,
  indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

find = function(array, predicate) {
  var el, i, len;
  for (i = 0, len = array.length; i < len; i++) {
    el = array[i];
    if (predicate(el)) {
      return el;
    }
  }
};

includes = function(array, el) {
  return array && indexOf.call(array, el) >= 0;
};

dtPredicate = function(slug) {
  return function(deviceType) {
    return deviceType.slug === slug || includes(deviceType.aliases, slug);
  };
};

exports.findBySlug = findBySlug = function(deviceTypes, slug) {
  return find(deviceTypes, dtPredicate(slug));
};

exports.normalizeDeviceType = function(deviceTypes, slug) {
  var ref;
  return (ref = findBySlug(deviceTypes, slug)) != null ? ref.slug : void 0;
};
